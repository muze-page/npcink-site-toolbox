import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.tsx";

ReactDOM.createRoot(document.getElementById("npcink_site_toolbox_census_count")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
